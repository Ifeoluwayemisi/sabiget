services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sabiget
    volumes:
      - db-prod-data:/var/lib/postgresql/data
    # Do not expose DB to host in production by default

  redis:
    image: redis:7

  migrate:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    env_file:
      - backend/.env
    command: ["sh", "-c", "npx prisma migrate deploy"]
    depends_on:
      - db
    restart: "no"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    image: sabiget-backend:prod
    env_file:
      - backend/.env
    # MEDIA_* variables (MEDIA_BUCKET, MEDIA_REGION, MEDIA_ENDPOINT,
    # MEDIA_ACCESS_KEY_ID, MEDIA_SECRET_ACCESS_KEY, MEDIA_PUBLIC_BASE_URL) are
    # read from backend/.env through env_file above. When unset the backend
    # fails closed with 503 MEDIA_NOT_CONFIGURED.
    ports:
      - "5000:5000"
    depends_on:
      - db
      - redis
      - migrate

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    image: sabiget-frontend:prod
    env_file:
      - frontend/.env
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  db-prod-data:
    driver: local
